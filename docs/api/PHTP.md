# PHTP API

Source: `library/PHTP.php`  
SHA-256: `a56bf3263770a6fd44ac22ec71f167afd5f8a2c32fb1714b78ec8f0cfaac7205`

Classes: `PHTP`

## Public methods

### `static configure(array $config = []): array`

Configure the account-level Authenticator service.

### `static key($length = 24, $mode = 'TOTP')`

Generate a Base32 secret.

### `static code($secret, $mode = 'TOTP', $digits = 6, $time = 30, $offset = 0, $algo = 'sha1')`

Generate an OTP/TOTP code. Offset is expressed in seconds.

### `static verify($otp, $secret, $mode = 'TOTP', $digits = 6, $time = 30, $offset = 0, $algo = 'sha1', $window = 0)`

Verify an OTP/TOTP. The optional window checks adjacent TOTP periods.

### `static url($account, $secret, $digits = null, $time = null, $issuer = null, $algo = null)`

Build a standards-compatible otpauth URI.

### `static enroll(string|int $account, array $options = []): array`

Begin enrollment or rotation without replacing an active factor yet.

### `static confirm(string|int $account, string|int $code): array`

Confirm a pending enrollment and atomically activate it for the account.

### `static authenticate(string|int $account, string|int $code): array`

Verify an Authenticator or one-time recovery code for an active account.

### `static status(string|int $account): array`

Return non-sensitive enrollment status.

### `static recovery(string|int $account, string|int $currentCode): array`

Replace recovery codes after proving possession of the current factor.

### `static disable(string|int $account, string|int|null $code = null, bool $force = false): array`

Disable the factor. Force mode is intended only for an already-authorized administrator.


Generated by `mystack docs:build`; do not hand-edit.
