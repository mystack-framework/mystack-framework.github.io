# PHAI API

Source: `library/PHAI.php`  
SHA-256: `c21a776948b7dc3ae59b95920a462e7c7086fb52007698eeb44f587eea809b7a`

Classes: `PHAI`, `PhaiBuilder`, `PhaiMemory`, `PHAI_AI`

## Public methods

### `static setAccounts(array $accounts)`

Sets AI provider accounts.

### `static setPriority(array $priority)`

Sets AI provider priority order.

### `static setModels(array $models)`

Sets AI models for providers.

### `static setTimeout(int $seconds)`

Sets the default timeout for AI requests.

### `static getModels(string $provider)`

Gets models for a specific provider.

### `static registerBridgeProcess($key, $process, $pipes)`

Registers a bridge process and its pipes.

### `static getBridgeProcess($key)`

Gets a registered bridge process.

### `static cleanup()`

Cleans up all registered bridge processes.

### `static serve(string $prefix = "/v1", ?string $apiKey = null)`

AI API SERVE (Universal Compatibility Bridge - Final)

### `static clusterAPI(string $path = "/v1/chat/completions", ?string $apiKey = null)`

See executable source and call sites for the behavioral contract.

### `static cluster(mixed $input, array $options = [])`

See executable source and call sites for the behavioral contract.

### `static getInstance(): PHAI`

See executable source and call sites for the behavioral contract.

### `static routes(string $path = "/mcp")`

See executable source and call sites for the behavioral contract.

### `static tool(string $name, string $description, array $schema, mixed $handler): PhaiBuilder`

See executable source and call sites for the behavioral contract.

### `static prompt(string $name, string $description, array $arguments, mixed $handler): PhaiBuilder`

See executable source and call sites for the behavioral contract.

### `static resource(string $uri, string $name, string $description, mixed $handler): PhaiBuilder`

See executable source and call sites for the behavioral contract.

### `static resourceTemplate(string $uriTemplate, string $name, string $description, mixed $handler): PhaiBuilder`

See executable source and call sites for the behavioral contract.

### `static alias(string $customAlias, string $targetMethod)`

See executable source and call sites for the behavioral contract.

### `static middleware(callable $middleware)`

See executable source and call sites for the behavioral contract.

### `handleRequest($data)`

See executable source and call sites for the behavioral contract.

### `static bridge(string $target, string $method, array $params = [], $options = [])`

See executable source and call sites for the behavioral contract.

### `__construct(array &$reference)`

See executable source and call sites for the behavioral contract.

### `middleware(callable $callback): self`

See executable source and call sites for the behavioral contract.

### `retries(int $amount): self`

See executable source and call sites for the behavioral contract.

### `set(string $key, $value, ?int $expiration = null, array $tags = [])`

See executable source and call sites for the behavioral contract.

### `remove(string $key)`

See executable source and call sites for the behavioral contract.

### `static call(string $provider, mixed $input, string $apiKey, array $options = [])`

See executable source and call sites for the behavioral contract.


Generated by `mystack docs:build`; do not hand-edit.
