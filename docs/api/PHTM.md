# PHTM API

Source: `library/PHTM.php`  
SHA-256: `8015e3f4fca1ef429b099659578fb57aa98831f1dbaa351ec5e485b43310f842`

Classes: `PHTM`

## Public methods

### `static setZone($timezone)`

Set the default timezone.

### `static getZone()`

Get the current default timezone.

### `static getTime($format = 'Y-m-d H:i:s')`

Get the current date and time in the specified format.

### `static setTime($timestamp, $format = 'Y-m-d H:i:s')`

Format a given timestamp according to the specified format.

### `static calculate($datetime1, $datetime2 = null)`

Calculate the difference between two date/times.

### `static modify($datetime, $modifier, $format = 'Y-m-d H:i:s')`

Modify a date/time string by adding or subtracting a specified interval.

### `static format($datetime, $outputFormat)`

Change the format of a date/time string.

### `static to12h($datetime, $outputFormat = 'g:i:s A')`

Convert a date/time string from 24-hour format to 12-hour format with a custom output format.

### `static to24h($datetime, $outputFormat = 'H:i:s')`

Convert a date/time string from 12-hour format to 24-hour format with a custom output format.


Generated by `mystack docs:build`; do not hand-edit.
