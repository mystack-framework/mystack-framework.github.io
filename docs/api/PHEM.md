# PHEM API

Source: `library/PHEM.php`  
SHA-256: `2c1f894f1e35598334db00b387cf15e489259cbd87f5b404332e3a90d72ee661`

Classes: `PHEM`, `PHEM_MailQueueHandler`

## Public methods

### `static smtp($smtpHost, $smtpPort, $smtpSecure)`

Configure SMTP settings.

### `static imap($imapHost, $imapPort, $imapSecure, $folder = 'INBOX')`

Configure IMAP settings.

### `static pop($popHost, $popPort, $popSecure, $folder = 'INBOX')`

Configure POP3 settings.

### `static smtpLogin($username, $password)`

Set SMTP login credentials.

### `static imapLogin($username, $password)`

Set IMAP login credentials.

### `static popLogin($username, $password)`

Set POP3 login credentials.

### `static smtpGet($filter, $limit)`

Retrieve emails using SMTP settings (alias for IMAP get).

### `static imapSend($from, $name, $to, $cc, $bcc, $subject, $message)`

Send an email using IMAP settings (alias for SMTP send).

### `static popSend($from, $name, $to, $cc, $bcc, $subject, $message)`

Send an email using POP3 settings (alias for SMTP send).

### `static imapGet($filter, $limit)`

Retrieve emails using IMAP settings.

### `static popGet($filter, $limit)`

Retrieve emails using POP3 settings.

### `static smtpSend($from, $name, $to, $cc, $bcc, $subject, $message)`

Send an email using SMTP settings.

### `static showLog()`

Display the SMTP log.

### `static queue($from, $name, $to, $cc, $bcc, $subject, $message, $delay = 0, $tries = 3)`

Queue an SMTP email for asynchronous delivery through the local console queue.

### `static queueSend(array $payload)`

Restore a queued SMTP configuration and deliver one email message.

### `static handle(array $payload): void`

See executable source and call sites for the behavioral contract.


Generated by `mystack docs:build`; do not hand-edit.
