# PHEV API

Source: `library/PHEV.php`  
SHA-256: `046d13cf9ad3a45068ca6afe2bee9a1c928af57d2545d980801bd7f17a299490`

Classes: `PHEV`

## Public methods

### `static allowWebWorker(bool $allow = true): void`

Explicit compatibility switch for hosts that intentionally run a socket loop in a web worker.

### `static initialize($path = '/websocket', $address = '0.0.0.0', $port = 8000)`

See executable source and call sites for the behavioral contract.

### `static start()`

See executable source and call sites for the behavioral contract.

### `static restart()`

See executable source and call sites for the behavioral contract.

### `static stop()`

See executable source and call sites for the behavioral contract.

### `static running(): bool`

See executable source and call sites for the behavioral contract.

### `static clients()`

See executable source and call sites for the behavioral contract.

### `static debugClients()`

See executable source and call sites for the behavioral contract.

### `static getHandler($message)`

See executable source and call sites for the behavioral contract.

### `static handler($requestPath, $action, $handler)`

See executable source and call sites for the behavioral contract.

### `static message($clientId, $message)`

See executable source and call sites for the behavioral contract.

### `static broadcast($message)`

See executable source and call sites for the behavioral contract.

### `static disconnect($clientId = null)`

See executable source and call sites for the behavioral contract.

### `static initHeaders()`

Initialize SSE headers

### `static sendSE($data, $event = null, $id = null)`

Send data to the client

### `static setRetry($milliseconds)`

Set a retry interval for the client

### `static stream(callable $callback, int $interval = 10000)`

Start streaming data continuously

### `static streamUInew(string $key, int $interval = 10000)`

See executable source and call sites for the behavioral contract.

### `static streamUI(string $name = '/streamui', int $interval = 10000)`

See executable source and call sites for the behavioral contract.


Generated by `mystack docs:build`; do not hand-edit.
